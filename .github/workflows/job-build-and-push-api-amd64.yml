name: build-and-push-api-image

on:
  workflow_call:
    inputs:
      tag:
        required: true
        type: string

jobs:
  build-and-push-api-image:
    runs-on: ubuntu-latest
    if: ${{ success() }}
    uses: ./.github/workflows/job-build-and-push-image.yml
    name: build and push api image to cr
    with:
      container-registry: "ghcr.io"
      code-folder: "api"
      image-name-with-tag: "activity_api:${{ inputs.tag }}"
      arch-types: "linux/amd64"
      build-multi-platform: false
      docker-file-name: "Dockerfile"
