
services:
  client:
    image: ghcr.io/acnicholls/activity_client:stage-amd64
    networks:
      activity:
        aliases:
          - client
  api:
    image: ghcr.io/acnicholls/activity_api:stage-amd64
    links:
      - db
    networks:
      activity:
        aliases:
          - api
  db:
    image: ghcr.io/acnicholls/activity_db:stage-amd64
    networks:
      activity:
        aliases:
          - db
    volumes:
      - db_data:/var/opt/mssql/data:rw
  proxy:
    image: ghcr.io/acnicholls/activity_proxy:stage-amd64
    ports:
      - 800:800
    links:
      - client
      - api
    networks:
      activity:
        aliases:
          - proxy

networks:
  activity:

volumes:
  db_data:
